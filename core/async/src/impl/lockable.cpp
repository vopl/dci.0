#include "lockable.hpp"
#include <cassert>

namespace dci { namespace async { namespace impl
{

    Lockable::Lockable()
    {
        assert(0);
    }

    Lockable::~Lockable()
    {
        assert(0);
    }

    void Lockable::lock()
    {
        assert(0);
    }

    void Lockable::unlock()
    {
        assert(0);
    }

}}}
